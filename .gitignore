# Dependencies
node_modules/
package-lock.json

# Development files
.DS_Store
*.log

# Don't commit the generated papers data (GitHub Actions will create it)
# papers-data.json

# Keep these files - they're needed for GitHub Actions to fetch papers
# server.js (needed by fetch-papers.js)
# journal-config.js (needed by fetch-papers.js)
# vercel.json (needed if deploying to Vercel)
# netlify.toml (needed if deploying to Netlify)
